# dependencies
node_modules

# next.js build output
.next/
out/
build/
dist/

# environment — DATABASE_URL lives here, never commit it
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo

# local sample data and brand material (kept on disk, not in the repo)
test files/

# SQL handed to the database owner to run on Neon (kept out of the repo)
neon-sql/

# editor / OS junk
~$*
.DS_Store
Thumbs.db

# subagent-driven-development scratch (ledger, briefs, review packages)
.superpowers/

#claude files
docs/
